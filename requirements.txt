# Post-processing environment (outside 3DEC).
# The strategy_C_3dec_*.py drivers run in 3DEC's embedded interpreter and need
# only numpy, which ships with 3DEC.

numpy>=1.21
scipy>=1.7          # exp_period_evolution.py: butter, filtfilt, welch
matplotlib>=3.5     # all figure scripts (always used with the Agg backend)
openpyxl>=3.0       # experimental .xlsx readers
pandas>=1.3         # process_tiltmeter.py only
